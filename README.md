Lập trình web TS. Dương Trần Đức
Photo Sharing App

Backend auth cookies:
- Set CLIENT_ORIGIN and NODE_ENV in .env (see .env.example)
- Frontend axios/fetch must send credentials (withCredentials: true / credentials: 'include')
